using System;

namespace ConsoleApp4
{
    class Loop
    { 
        //sum of n even and odd numbers
        public void series(int n)
        {
            int i, etotal, ototal;
            etotal = ototal = 0;
            Console.WriteLine("List of even number");
            for(i=1;i<=n;i++)
            {
                {
                    if (i % 2 == 0)
                    {
                        
                        etotal += i;
                    }
                    
                    {
                        odd = i;
                        etotal+ = 

    class Program
    {
        static void Main(string[] args)

        {
                int num;
                Loop obj = new Loop();
                Console.WriteLine("Enter any number");
                num = Convert.Total32(Console.ReadLine());
                //named arguments
                obj.series(n: num);

        }
    }
}
